import 'package:flutter/material.dart';

class VoiceCommandScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Voice Command')),
      body: Center(child: Text('Voice Command Screen')),
    );
  }
}
